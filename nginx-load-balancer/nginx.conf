upstream backend {
    server server:8080;    
}

server {
    listen 80;

    resolver 8.8.8.8 valid=5s;
    
    include /etc/nginx/mime.types;

    location / {
        proxy_pass http://backend/;
    }
}